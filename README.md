A Simple script to be run as a service on a Raspberry Pi to read out a temperature sensor, report the readings to a MQTT broker and adjust the angle of a servo motor with a PID controller.
The script is intended for monitoring and regulation of the flow temperature of a floor heating system via a mixing valve.
